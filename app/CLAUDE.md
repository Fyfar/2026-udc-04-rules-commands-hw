# CLAUDE.md

See @AGENTS.md for structure, commands, code style, architecture, and
guardrails — that file is the single source of truth for this app.

Claude Code note: run `npm test` after every change to `src/reducer.ts`.
